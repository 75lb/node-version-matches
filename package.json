{
  "name": "node-version-matches",
  "author": "Lloyd Brookes <75pound@gmail.com>",
  "version": "1.0.0",
  "description": "Returns true if the current node versions matches the given semver",
  "repository": "https://github.com/75lb/node-version-matches.git",
  "keywords": [
    "node",
    "version",
    "semver",
    "check",
    "test"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=6"
  },
  "scripts": {
    "test": "test-runner test.js"
  },
  "devDependencies": {
    "test-runner": "^0.5.0"
  },
  "dependencies": {
    "semver": "^5.5.0"
  }
}
